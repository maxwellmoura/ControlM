specVersion: "v1"
serviceId: "controlm"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "controlm-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
