rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Por padrão, tudo bloqueado.
    match /{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.token.admin == true;
    }
  }
}
